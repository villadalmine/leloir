# Go
bin/
dist/
*.test
*.out
vendor/
poc/poc

# IDE
.idea/
.vscode/
*.swp

# Env
.env
*.env.local

# OS
.DS_Store

# k8s / helm
*.kubeconfig
kubeconfig

# Private — documentación personal, planes, milestones (quedan solo en local)
docs/

# Claude Code — memoria y settings locales
.claude/

# Meta local — no publicar en el remoto
/CLAUDE.md
/INDEX.md
/index.md

# Kubernetes secrets — NUNCA commitear, crear con kubectl
secrets/
*.secret.yaml
*-secret.yaml
*-credentials.yaml
acme-dns-account.json

# Archivos locales del entorno del usuario
OPENBSD_UPGRADE_CHECKLIST.txt
openbsd-upgrade-helper.sh
local-*
